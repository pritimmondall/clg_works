// given an array of numbers containing n+1 intigers where each intiger is between 1 and n inclusive. proof that atleast one dublicate number must exist return the dublicate number. Constrain : don't modify the array dont use extra space

#include <stdio.h>


int findDuplicate(int arr[], int n) {
    for(int)
}

int main() {
    int arr[] = {3, 1, 3, 4, 2};
    int n = sizeof(arr) / sizeof(arr[0]);

    int duplicate = findDuplicate(arr, n);
    printf("Duplicate number is: %d\n", duplicate);
    return 0;
}
